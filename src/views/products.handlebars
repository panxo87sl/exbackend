<h2>Lista de productos</h2>
<ul>
  {{#each products}}
    <li>
      <strong>{{title}}</strong>
      - Precio: ${{price}}
      - Stock:
      {{stock}}
      <strong>---</strong>
      - SKU:
      {{code}}
      - IDMongo:
      {{_id}}
    </li>
  {{/each}}
</ul>