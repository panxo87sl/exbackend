<h2>Lista de productos</h2>
<ul>
  {{#each products}}
    <li>
      <strong>{{title}}</strong>
      - Precio: ${{price}}
      - Stock:
      {{stock}}
      <strong>---</strong>
      - Code:
      {{code}}
      - SKU:
      {{_id}}
    </li>
  {{/each}}
</ul>