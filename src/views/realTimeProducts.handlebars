<h2>Lista de productos en tiempo real</h2>

<ul id="product-list">
  {{#each products}}
    <li>
      <strong>{{title}}</strong>
      - Precio: ${{price}}
      - Stock:
      {{stock}}
    </li>
  {{/each}}
</ul>