<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Home - Lista de productos</title>
  </head>
  <body>
    <h1>Lista de productos</h1>

    <ul>
      {{#each products}}
        <li>
          <strong>{{title}}</strong>
          - Precio: ${{price}}
          - Stock:
          {{stock}}
        </li>
      {{/each}}
    </ul>

  </body>
</html>